const db = require('./index');

module.exports = async function getReferences() {
    // await db;
    // const {models} = db;
    // const US = await models.Country.findOne({ where: { name: 'United States' } });
    // const France = await models.Country.findOne({ where: { name: 'France' } });
    // const Italy = await models.Country.findOne({ where: { name: 'Italy' } });
    // return US, France, Italy;
}
